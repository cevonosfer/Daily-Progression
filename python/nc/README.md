# usage: python file.py -t {ip or hostname} -p {port(s)} -m {message} -f {filename} 
# if you want to create a server on the given address use -l
# if you want to port scan a server on the given address use -zv